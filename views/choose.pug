extends layout

block content
  h1= title
  - let image_names = ["clock1.jpg","clock3.jpg","clock2.jpg","clock4.jpg","clock5.jpg"]
   -for(let i=0; i<3; i++){
    -let random= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[random]}` alt=`Image Name clock${random}` height="300px" width="300px")
    -}