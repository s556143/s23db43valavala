extends layout

block content
  h1= title
  - var items= results
   div.container-fluid
    div.row 
     each clock,index in results
      div.col-sm-2(onclick=`buy(${index})`) #{clock.name}
      div.col-sm-2(onclick=`buy(${index})`) #{clock.price}
      div.col-sm-2(onclick=`buy(${index})`) #{clock.color}
      - let destination = `dispatch(\"/clock/update?id=${clock._id}\")`
         div(class="row-sm-2", class="m-1") Item #{clock.name}(#{clock.price}) is #{clock.color}
         button.btn( class="btn-primary ml-1", onclick= destination) Update

       
      br
      hr
      
    

    script. 
       function buy(index)
       {
        alert(`you purchased Item ${index+1}`);
       }

       function dispatch(path)
       {
        window.location.href = path;
       }

